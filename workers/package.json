{
  "name": "holanu-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute holanu-db --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute holanu-db --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.78.7"
  }
}
